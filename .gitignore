node_modules/
data/
workspaces/
.DS_Store
*.log
tmp_*.js
tmp_append.txt
config.json

# v0.13 修复：3 处 debug-logger 原代码漏一层 ..，把 acms-llm-debug.log 写到 server/data/，已修
# 此目录是历史错误产物，不入仓库
server/data/

# Local test output
server_out*.txt
tmp/

# Server stdout/stderr (本地运行产出)
server_out.txt
server_err.txt

# 探索性原型/草图 (不入仓库，保留本地工作)
sketches/

# 本地调试测试残留（不入仓库）
start_gbk.bat
test-chcp.bat
test-set.bat
test-set-in-utf8.bat
test-start-clean.bat
test_export_result.docx
test_md2docx.js
tmp_brief.json
export_示例.docx

# v0.77: 兄弟 agent 调研 DDG 抓取的本地 probe 脚本（不入仓）
scripts/probe-*.js
scripts/test-merge-search.js

# v0.77: WireGuard 备份目录（VPN 切换到 OpenVPN 后已清，含 client.conf/ovpn/msi，~8.9MB）
wireguard/

# v0.77: bge 嵌入模型目录（319MB,不入仓；运行期 .cache + 模型权重均本地生成）
models/

# Office V3 构建临时目录（scripts/build-office-v3.js 产物中间态）
.build/

# 用户生成/上传的办公文件（不入仓）
server/public/office/
server/public/generate/assets/
